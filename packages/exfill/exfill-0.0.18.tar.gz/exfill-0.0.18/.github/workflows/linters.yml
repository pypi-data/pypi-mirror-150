name: Linters

on:
  pull_request:
    types: [opened, reopened]
    branches:
      - main

jobs:

  black:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: black
        uses: psf/black@stable
